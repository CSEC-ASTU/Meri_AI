# Server

Minimal FastAPI app with a Hello World endpoint.

Run locally:

```bash
python -m pip install -r requirements.txt
uvicorn main:app --reload --host 127.0.0.1 --port 8000
```

Then open: http://127.0.0.1:8000/hello
